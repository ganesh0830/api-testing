## Running the Rest API Testing Framework
To run the Rest API Testing Framework run the following command:

### Examples
Running the framework with a single test file: 

```
node .\app.js --testspath="./tests/fakeAPI/test01.json"
```

Example of using the framework to execute all tests within a folder:

```
node .\app.js --testspath="./tests/fakeAPI"
```